;;; -*- lexical-binding: t -*-

;;; Keycast

;; TODO: Load and unload this on demand? Usually won't want it by default since
;;       it's another package to load at startup.

(use-package keycast)

(provide 'i-keycast)
